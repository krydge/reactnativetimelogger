import { View, Text } from "react-native";
import { useState } from "react";

export default function StatisticsView(params) {

    return (
        <View>
            <Text>StatisticsView</Text>
        </View>
    );
}